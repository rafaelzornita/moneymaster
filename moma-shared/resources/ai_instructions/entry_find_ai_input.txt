Consider today date {{today}} week beginning at monday and these categories: {{categories}}

Interpret the user message and respond in compressed JSON using the mandatory following template without line breaks or spaces.
{
  "filter": [{          /See filter array topic 
	"title": "",        /Fill if user have informed some kind of description. Accepts just one word
	"value": 0,			/Valid if used beside another value. Remove field if zero.
	"initial_date": "", /Required. Period start
	"final_date": "",   /Required. Period end
	"operation": "",    /Optionally Fill with D for expense and C for income
	"categories": [],   /Fill with previously informed categories id as needed
	"observation": ""   /Accepts just one word
  }],
  "user_answare":"",   /Not fill if its possible to fill the filter field
  "canceled":false     /True if ocurred some problem or user message is not enough to fill create at least one filter object
}

If user wanna ask something about registered data, extract the context and subject, converts the principal words to singular and disregard extra information that was informed to aditional operation
Imagine user wants to return data but it dont remember exactly how it was stored so we need to fragment user input text to get wanted results by parts
and to do it we have Filter array:
Objects in array will be used to seach data on costumer SQL database. 
Fields inside object are processed with AND operation and OR operation is applied between array items.
Know that Data on database was possible not stored as user has informed on input so to find out the records, follow these rules: 
0. Split the text in many relevant words or expressions and then generate many 'filter' different items, one item for 'title' and other one for 'observation' to the same value to capture separate and grouped keywords from the message. Generate how many items you can but at least 3 items with text variations verbal times, substantive to verb and vice versa.
1. initial_date and final_date are required so include them in any generated item
2. If period was not informed use initial date as 30 days ago and final date as today
3. Ignore title and observation if a category was informed
4. title and observation fields, each item in the filter array must fill only one of these fields. If one field is present, the other must be empty in the same item. Fields can be present many times since diferent items.
5. Priorize using title instead observation field.
6. generate many array items varying the possible filter combinations
7. Avoid using category name in other search fields
8. Keep just the filled and required fields, remove empty optional fields and zero lenght fields.